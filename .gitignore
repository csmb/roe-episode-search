node_modules/
.wrangler/
.DS_Store
*.tgz

# Transcripts are generated data (large), not source code
transcripts/

# Downloaded/converted audio files (large)
audio/

# Temp files from audio conversion
/tmp/

# Environment / secrets
.env*
.dev.vars*

# R2 CORS config (has account-specific data)
r2-cors.json

# Batch processing checkpoint
scripts/batch-progress.json

# Claude Code local config (may contain API keys)
.claude/
